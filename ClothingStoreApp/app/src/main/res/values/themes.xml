<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ClothingStoreApp" parent="android:Theme.Material.Light.NoActionBar" />
    <!-- Base application theme. -->
    <style name="Base.Theme.ClothingStoreApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Font family -->
        <item name="android:fontFamily">@font/inter_regular</item>

        <!-- ... -->
    </style>

    <style name="CustomAlert" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>