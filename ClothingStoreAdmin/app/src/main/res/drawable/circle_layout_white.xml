<?xml version="1.0" encoding="utf-8"?>

<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/placeholderColor">
    <item>
        <shape android:shape="oval">
            <solid android:color="@color/white" /> <!-- Màu nền của hình tròn -->
            <corners android:radius="1000dp" /> <!-- Đặt bán kính lớn để tạo thành hình tròn -->
            <stroke
                android:width="1dp"
                android:color="@color/placeholderColor"/>
        </shape>
    </item>
</ripple>